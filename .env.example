# Valhalla upstream for POST /api/route (server-only).
# The app POSTs bicycle traces to /trace_attributes for road map matching.
# https://valhalla.github.io/valhalla/api/map-matching/
VALHALLA_BASE_URL=https://valhalla1.openstreetmap.de
